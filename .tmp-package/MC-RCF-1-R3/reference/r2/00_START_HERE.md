# MC-RCF-1-R2｜恢复权限边界，修好执行链，完成原定基础闭环
## 外部实现 Agent 任务书 · 2026-09-21

**本轮不是新功能轮。保留 Baritone、原生合成、生命周期 intent 等有效成果，纠正 R1 的越界修复和不可信验收，然后完成 MC-RCF-1 原定 G4/G5 与 fresh replay。**

你负责内部设计、代码、自动化、LIVE、修复和可审查交付。本包不提供未经 Fabric 验证的生产补丁，不替你固定内部 schema。它给出精确起点、已知反例、必须保持的行为、执行顺序和判定条件。

## 1. 起点

| 项目 | 规定 |
|---|---|
| 仓库 | `ceyirelehe47/mc-bot` |
| 继续分支 | `experiment/rc-foundation-v1` |
| 本轮审查基准 | `8626013b4a829900bc001f00349e3984130f7d21` |
| 旧交付 | `deliveries/mc-rcf1/`、`deliveries/mc-rcf1-r1/`，保留历史事实 |
| 本轮交付 | `deliveries/mc-rcf1-r2/` |
| 路线 | MC 1.21.3、独立 Bob Real Client、受控 Baritone、正常客户端 API、同一语义动作接口 |
| 源码权威 | Git 跟踪的 overlay / installer / 构建资源 / 工具；其他树是生成物 |

开工再次读取 origin，核验 HEAD、工作树和未推提交。远端仍为本基准则直接继续；若已前进先映射差异，不能覆盖新工作或机械重放旧结论。已有不明改动或其他写作者时保全现场、停止竞争写入。不要 reset --hard、git clean、重建孤儿分支、切回旧 0.8/master、force push 或回引含泄露秘密的旧历史。

R1 实際结果是 PARTIAL：G4 阻塞、G5/replay 未运行、A08 失败，checker 存在离线可复现误接受。此前自报 PASS 不构成本轮免检。独立审查原文和四组反例在 `reference/review_8626013/`；它们是历史审查输入，不得改成“通过版”。

## 2. 本轮重点

1. **恢复发现与执行的区别。** 全向 awareness 只读；转向后新的、合法且一致的客户端准星帧才有资格让 durable opportunity 出生。不要靠 sweepRegister、假来源标签或 inspect 隐式写入补机会。
2. **先修确定性错误。** 主包食物 PICKUP 后不得穿透到消费收尾；普通成功/失败/格式错误都必须停止导航与后续点击；stop 无记录仍须查孤儿实例，另一角色成功不能抹掉失败。
3. **把验证连到实际实现。** 同一个最终 checker 验真实正例及其变异副本；V10 调用真正的避难策略；NOT_RUN 不计 PASS，测试前置条件本身需要被证明。
4. **形成可重复的成果。** 单次诊断核心链 → 最终候选完整矩阵与 G4 五次 → 两次自然过夜 → 新目录重建复验。单步挖掉一个方块不是终点。

## 3. 阅读顺序与优先关系

先读本页和 `AGENT_PROMPT.txt`，再按以下顺序：

- `docs/01_OPPORTUNITY_BOUNDARY_AND_HANDOFF.md`：机会出生、目标准备与 stale 收敛。
- `docs/02_EXECUTION_AND_LIFECYCLE.md`：进食、全出口收尾、生命周期、数量与主体归因。
- `docs/03_CHECKER_AND_COVERAGE.md`：真实判定器、覆盖纠偏和反测试。
- `docs/04_RUN_ORDER_AND_ACCEPTANCE.md`：最小链、计分矩阵、限时与停止。
- `docs/05_DELIVERY_AND_REPLAY.md`：产物身份、原始脱敏事实与交付。
- `docs/06_SOURCE_MAP.md`：定位入口和“事实/推导/新要求”区分。

R2 明确更新的起点、执行顺序与预算优先；R1 修复要求和原 MC-RCF-1 完整业务验收继续有效，原文在 `reference/`。**本轮明确允许：安全与关键边界负例通过后，在完整回归收口前做一次非计分诊断核心链。** 这是为了早发现串联缺陷，不是豁免任何最终验收，也不是允许 G5 提前开始。

历史 MC-2A0.8 交接只保留真实客户端、全向只读、fresh crosshair、无透视、Screen 与凭证边界；旧进度、文件白名单、固定测试数量、DSH 恰好 29、旧 attestation 开工门和“ChatGPT 先写整份生产补丁”不适用于本轮。

## 4. 执行责任与资源

唯一协调者负责所有真实起停、租约、fixture 和 LIVE。subagent 可写代码、跑隔离离线测试及只读审查，不得另起游戏。全机本轮最多一套受管测试 server + 一套 Bob client；GameTest 的服务端也需串行纳入资源协调。用户普通客户端不属于可以随意终止的测试实例。

允许必要的控制器、驱动、tracker、导航、GUI、桥接私有接口、测试及 installer 修改；调用者同步更新。只补本链所需的目标准备，不扩展为新自主规划系统。继续禁用未验收 smelt/时代脚本，不新增铁器、加工机器、物流平台、战斗或其他 Mod Adapter。

## 5. 完成与停止

本轮新授权最多 **12 小时主动实施和验证窗口**，从现场核验记录起算；不是耗时保证。保留前轮投入与失败台账，不把同一失败换名称后当第一次。

同一关键失败签名最多三轮有依据的修复复测；两次同条件无进展游戏尝试即停止循环转离线诊断。安全/边界不满足不得继续计分。涉及需要放弃真实客户端、透视边界、升级版本或重写共享历史时报告决策阻塞，不自行换路线。阶段性推送不是停止命令；预算和授权仍有效就继续下一阶段。

全部必需门、证据和 fresh replay 成立，仅可提交 `READY_FOR_REVIEW`，正式接受仍由独立审查决定。缺项则 PARTIAL/BLOCKED。账户轮换等外部安全条件单列，不冒称完成，也不阻断不使用泄露凭证的安全本地修复。交付后安全停止本轮实例，不再后台续跑。
