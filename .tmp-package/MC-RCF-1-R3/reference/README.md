# 参考材料与优先关系

这些是完整保留的历史原文，不是新一轮 PASS 的证据。reference/original 为原 MC-RCF-1 八份正文；reference/r1、reference/r2 为两轮修复要求；review_8626013、review_ff29078 为独立审查与反例。FILE_PROVENANCE.json 逐文件登记原始副本哈希。

当前生效顺序：R3 正文明确更新 > R2/R1 修复要求 > 原完整业务矩阵。历史基准、目录、预算被本轮替代；无明确修改的业务条件不删除。不得因参考文件含旧候选而回切分支。

历史 0.8 交接只保留真实客户端、无透视、全向只读、定向准星、Screen 与安全原则；不恢复旧白名单、固定测试数量、DSH 恰好 29、旧 attestation 开工门或 ChatGPT 必须预制生产补丁的流程。

review_ff29078 下的源码是有已知缺陷的审查对象，严禁复制覆盖生产代码。synthetic_inputs 下的所有 JSON 均为故意构造的无效证据，不是 Minecraft LIVE。历史 Java/Python 探针用于复现旧缺陷；在临时目录运行，不能改写参考原件。

引用历史资料发现表述冲突时，登记冲突与裁决，不静默改写原文。本包要求的新硬约束是下一轮任务规定，不是声称它们已经实现。
