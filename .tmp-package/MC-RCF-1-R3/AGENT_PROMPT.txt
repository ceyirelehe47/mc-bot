执行 MC-RCF-1-R3。你负责在当前项目独立实现、测试、修复、推送并交付，不是只写一份新计划。

仓库 ceyirelehe47/mc-bot；继续 experiment/rc-foundation-v1；本包基准 ff29078ab3cd52d6cfceb928344c90e0c25ab2a7。开工刷新origin并保全未提交/未推工作；不得硬回退、重建孤儿分支、force push或覆盖其他写作者。新交付 deliveries/mc-rcf1-r3/。

先验证本包：python tools/verify_package.py。先读00_START_HERE、docs、原完整03_ACCEPTANCE和review_ff29078。历史源码是有缺陷的被测副本，禁止覆盖生产。原R1/R2业务条件继续；0.8旧进度/白名单/DSH29/固定计数和旧attestation门不生效。

这轮不扩新功能。保留受控Baritone、原生合成和已连通核心链。实现线关闭实际消费归因、G5正常入洞/封口/工具保留、纯读有界查询、所有同角色实例stop核验和恢复债务；验证线补真实cursor/ledger/加载身份/完整事件，主checker从事实重算，不能靠true或reason字符串。能分工则验证角色不得改生产事实；没有独立角色说明二次自审。

最重要的禁令：G4/G5 armed后无tp/give/setblock/改时间难度天气/清怪/复活/keepInventory/睡觉跳夜；Bot/LLM不读oracle隐藏答案。准备者、控制者、独立oracle隔离。G5确有上层LLM通过正式语义接口规划，固定Python剧本只能算技能诊断。查询失败/不在洞中/空包清晨不算安全或保存进度。

先补离线正反例和假进程故障，再用唯一一套游戏做非计分诊断核心链与洞内连续放置小切片。之后固定最终行为候选，完成原C/N/I/A/L/V、五次G4（每次12分钟）、两次自然G5（每次建议35分钟）、从锁定上游+installer空目录生成的新部署fresh replay。不要只修seal就直接等夜。正常主包进食成功不等于外部清物不能冒充消费；需要实际正常消费完成见证。

包内probe_checker_cli对旧checker预期检出7次误接受并非零退出；历史格式拒绝不够，必须另用本轮完整真实正例及格式正确语义变异经同一个最终入口验证。异常、超时、用法错误不等于明确拒绝。V10测真实策略，不在测试内另写正确if。I08真Tom’s转移、C08授权共存和所有子条件不得被“等价覆盖”标题吞掉。

同一真实game/control session可连续跑多run，记录真实事件区间，不为制造五个session多开/重连。全机最多1受管测试server+1Bob，GameTest串行；subagent不得启动游戏或抢租约。用户客户端/世界不触碰。无记录和有记录+额外实例均要核验；身份不明BLOCKED不批杀。

内部schema由你设计。原始事实完整且版本语义未受影响的单项可重算复用；缺事实重跑，不补true、不跨候选拼五次。代码/配置/策略改变建立新候选；运行时加载哈希不是硬编码C或仅磁盘待部署文件。报告引用的本轮IA/G5/replay证据必须真正落盘推送，失败也保存。

主动实施验证窗口最多12小时。同一关键失败最多三轮有效修复；两次同条件无净进展先停相关LIVE转诊断，不能换标签/seed/多开绕过。阶段push不是结束指令；未触发停止且预算仍在则继续。触发阻塞诚实交PARTIAL/BLOCKED；全部满足才READY_FOR_REVIEW，不能自封通过或冻结。

交付后通过唯一入口停止并确认本轮所有实例退出。未净化wire/凭证/state/第三方JAR不进Git；历史外部密钥不测试、不使用，账户轮换单列。最终回复写完整候选/远端HEAD、真实门状态、复现入口、未完成和安全停机结果。
